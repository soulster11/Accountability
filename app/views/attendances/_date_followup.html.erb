<p><%= select(:service, :id, @services.collect { |s| [s.printable_date, s.id] },
  { :include_blank => true }) %></p>

<%= observe_field :service_id, :url => { :action => :change_date_followup },
  :with => "'&group_id=' + $('group_id').value + '&service_id=' + $('service_id').value" %>
